services:
  - type: static
    name: swp-scaffolding
    env: static
    buildCommand: npm install && npm run build
    publishDir: dist
    autoDeploy: true
    envVars:
      - key: NODE_VERSION
        value: 20.0.0
